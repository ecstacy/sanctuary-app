<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ (API 33) requires runtime permission for notifications.
         The @capacitor/local-notifications plugin requests this at the time
         we schedule the first notification; manifest declaration is required
         for the system to surface the prompt at all. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for daily-repeating local notifications to survive
         device reboots. Without this, the user's morning reminder
         disappears every time they restart their phone. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- NO exact-alarm permission, deliberately.
         SCHEDULE_EXACT_ALARM is denied by default from Android 14 for our
         targetSdk (36) — the user has to find the "Alarms & reminders" toggle
         in system settings, and reminders silently never fire until they do.
         USE_EXACT_ALARM is not an alternative: it's reserved for apps whose
         core function is alarms/calendars, and claiming it for a wellness app
         invites a Play rejection.
         A practice reminder tolerates a few minutes of drift, so we schedule
         inexact alarms (see useNotifications.js) and need neither permission
         nor a Play declaration. Don't re-add this. -->

    <!-- allowBackup=false: the WebView's storage holds the Supabase session
         (access + refresh tokens) and the PKCE verifier. With backup enabled,
         `adb backup` or a cloud backup can lift a logged-in session off the
         device. Nothing here benefits from being backed up — practice data
         lives in Supabase and re-syncs on sign-in. dataExtractionRules /
         fullBackupContent are declared too so Android 12+ and older devices
         both honour the opt-out. -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTop"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.sanctuary.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
